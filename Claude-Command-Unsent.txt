Can you modify the scheduled messages widget so that it's default is the same size as a chat widget, also make it simpler on the main widget. just have a list of all the scheduled messages at the top (or say No messages scheduled) and then have a button at the bottom that says 'Create new Message'.  when you click that button all of the form elements which are currently in the widget will instead be in a popup.  This will allow you to see the entire form without scrolling. once the user is finished filling in the form, they hit the create button and the new scheduled message should show up in the list