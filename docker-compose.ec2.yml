# Docker Compose for EC2 production with host nginx
# Only runs the backend - nginx is managed by the host

services:
  backend:
    image: dallascaley/breakroom-backend:latest
    working_dir: /app
    ports:
      # Expose to localhost only (host nginx proxies to this)
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
      DB_NAME: ${DB_NAME}
      CORS_ORIGIN: ${CORS_ORIGIN}
      SECRET_KEY: ${SECRET_KEY}
      # AWS SES for email
      AWS_REGION: ${AWS_REGION}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      # Redis configuration
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      REDIS_KEY_PREFIX: ${REDIS_KEY_PREFIX:-prod}
      # S3 configuration
      S3_BUCKET_NAME: ${S3_BUCKET_NAME}
      S3_BUCKET_REGION: ${S3_BUCKET_REGION}
      # Square
      SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT}
      SQUARE_APPLICATION_ID: ${SQUARE_APPLICATION_ID}
      SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN}
      SQUARE_APPLICATION_SECRET: ${SQUARE_APPLICATION_SECRET}
      SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID}
      SQUARE_WEBHOOK_SIGNATURE_KEY: ${SQUARE_WEBHOOK_SIGNATURE_KEY}
      SQUARE_WEBHOOK_NOTIFICATION_URL: ${SQUARE_WEBHOOK_NOTIFICATION_URL}
      SQUARE_PRO_PLAN_VARIATION_ID: ${SQUARE_PRO_PLAN_VARIATION_ID}
      TOKEN_ENCRYPTION_KEY: ${TOKEN_ENCRYPTION_KEY}
      # Firebase (FCM push notifications)
      FIREBASE_SERVICE_ACCOUNT_JSON: ${FIREBASE_SERVICE_ACCOUNT_JSON}
      # EmailNanny OAuth proxy
      EMAILNANNY_CLIENT_ID: ${EMAILNANNY_CLIENT_ID}
      EMAILNANNY_CLIENT_SECRET: ${EMAILNANNY_CLIENT_SECRET}
    volumes:
      # Mount frontend dist so backend can read index.html for OG tag injection
      - /var/www/prosaurus.com:/app/dist:ro
    restart: unless-stopped
    command: sh -c "npm run start"
